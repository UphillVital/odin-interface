# GIT BRANCHES

## Основні гілки
- `dev` — робоча гілка розробки;
- `main` — стабільна / публічна база.

## Правило
Поточна робота ведеться у `dev`.

## Перевірка гілки
```bash
git branch
git status
```

## Важливо
Перед push завжди перевіряти, що ти у правильній гілці.
