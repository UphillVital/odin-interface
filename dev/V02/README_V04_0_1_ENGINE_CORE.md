# ODIN V04.0.1 — ENGINE CORE

Foundation layer for V04.0 Consolidation.

Adds:
- `odin_engine.js`
- `window.ODIN_ENGINE`
- Engine Core UI block in `admin.html`
- `ENGINE TEST`
- `DEPENDENCY CHECK`

V04.0.1 does NOT replace the stable V03 pipeline.

## Git
git add dev/V02/ ODIN_TREE_PROJECT_v1/
git commit -m "Add V04.0.1 engine core"
git push origin dev
