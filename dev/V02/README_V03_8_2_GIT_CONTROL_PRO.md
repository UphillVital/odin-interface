# ODIN V03.8.2 — GIT CONTROL PRO

PRO-версія Git Control: grouped plan, reason per file, custom commit message, clean command preview, exclude path, warnings.

## Git

git add dev/V02/ ODIN_TREE_PROJECT_v1/
git commit -m "Upgrade V03.8.2 git control pro"
git push origin dev
